import styled from "styled-components";

export const NavLinkStyled = styled.div`
    color: #D5CEA3;
    cursor: pointer;
    margin: 0 20px;
    height: 60px;
    display: flex;
    align-items: center;


    &:hover{
        transform: scale(1.05);
    }

`